- hosts: docker
  tasks:
   - name: Install NTP
     yum: name=chrony state=present
   - name: Make sure ntpd running.
     service: 
        name: chronyd 
        state: started 
        enabled: yes
